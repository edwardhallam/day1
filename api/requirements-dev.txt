# Development and test dependencies — NOT included in the production Docker image.
# Install with: pip install -r requirements.txt -r requirements-dev.txt

pytest>=9.0.3,<9.1
pytest-asyncio>=1.3,<1.4       # async test support (asyncio_mode = auto)
pytest-cov>=7.1,<7.2             # branch coverage reporting (NORM-REQ-012)
anyio[trio]>=4.0.0,<5.0            # async test runner backend
respx>=0.22.0,<1.0                 # httpx mock transport for ParcelAPIClient tests
factory-boy>=3.3.0,<4.0            # test fixture factories (optional)
httpx>=0.28.0,<1.0                 # AsyncClient for FastAPI test routes (Phase 8)
